# @package _global_
#package global is neccessary!
defaults:
  - multi_same_classes_convnext_train_finetune.yaml

module:
  network:
    model:
      freeze_backbone: False
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-09-30_110548/callback_checkpoints/last.ckpt

datamodule:
  limit_samples: True