# @package _global_
#package global is neccessary!
defaults:
  - multi_same_classes_convnext_train_finetune.yaml

module:
  network:
    model:
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-09-XX_XXXXXX/callback_checkpoints/last.ckpt

datamodule:
  limit_samples: True