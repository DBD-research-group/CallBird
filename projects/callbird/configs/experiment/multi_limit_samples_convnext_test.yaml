# @package _global_
#package global is neccessary!
defaults:
  - multi_same_classes_convnext_test.yaml

module:
  network:
    model:
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-10-02_095859/callback_checkpoints/last.ckpt

datamodule:
  limit_samples: True