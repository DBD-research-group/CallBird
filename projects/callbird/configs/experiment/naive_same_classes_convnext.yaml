# @package _global_
#package global is neccessary!
defaults:
  - naive_same_classes.yaml

module:
  network:
    model:
      _target_: projects.callbird.src.models.ConvNextSingleLayer.ConvNextSingleLayer
      pretrain_info: null
      freeze_backbone: True
      variant: three
      num_combined_classes: ${module.network.num_combined_classes}

logger:
  wandb:
    version: callbird_naive_same_classes_convnext_${start_time} 