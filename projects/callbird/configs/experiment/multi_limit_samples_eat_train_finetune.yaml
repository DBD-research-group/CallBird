# @package _global_
#package global is neccessary!
defaults:
  - multi_same_classes_eat_train_finetune.yaml

datamodule:
  limit_samples: True

module:
  network:
    model:
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-09-XX_XXXXXX/callback_checkpoints/convnext_callbird_09.ckpt

trainer:
  devices: [3]