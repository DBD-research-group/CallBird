# @package _global_
#package global is neccessary!
defaults:
  - multi_same_classes_eat_train_finetune.yaml

datamodule:
  limit_samples: True

module:
  network:
    model:
      freeze_backbone: False
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-09-30_110650/callback_checkpoints/last.ckpt

trainer:
  devices: [3]