# @package _global_
#package global is neccessary!
defaults:
  - naive.yaml

module:
  network:
    num_combined_classes: 98

datamodule:
  calltype_map: /workspace/projects/callbird/call_type_mappings/same_space_map
  unknown_ebird_code: NA
  num_combined_classes: ${module.network.num_combined_classes}

logger:
  wandb:
    version: callbird_naive_same_classes_${start_time} 