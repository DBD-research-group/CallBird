# @package _global_
#package global is neccessary!
defaults:
  - naive_song_merge_eat.yaml

train: True

datamodule:
  _target_: projects.callbird.src.datasets.naive.NaiveTrainDataModule.NaiveTrainDataModule
  filter_naive: /workspace/projects/callbird/blacklists/song_merge_train.txt

trainer:
  devices: [0]