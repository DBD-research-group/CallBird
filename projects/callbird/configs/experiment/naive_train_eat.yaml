# @package _global_
#package global is neccessary!
defaults:
  - naive_common.yaml
  - override /module/network: eat_soundnet.yaml

train: True
test: False

datamodule:
  _target_: projects.callbird.src.NaiveTrainDataModule.NaiveTrainDataModule

network:
  model:
    num_classes: 198

trainer:
  max_epochs: 50