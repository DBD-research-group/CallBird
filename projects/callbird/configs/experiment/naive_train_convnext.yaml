# @package _global_
#package global is neccessary!
defaults:
  - naive_common.yaml

train: True
test: False

datamodule:
  _target_: projects.callbird.src.datasets.naive.NaiveTrainDataModule.NaiveTrainDataModule

network:
  model:
    num_classes: 56
    checkpoint: DBD-research-group/ConvNeXT-Base-BirdSet-XCL