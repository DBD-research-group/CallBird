# @package _global_
#package global is neccessary!
defaults:
  - shared.yaml

module:
  _target_: projects.callbird.src.module.multitask_module.MultiTaskModule.TODO

datamodule:
  _target_: projects.callbird.src.TODO.TODO
  transforms:
    target_columns: ["labels_ebird"]

logger:
  wandb:
    group: callbird_naive
    version: callbird_naive_${start_time} 