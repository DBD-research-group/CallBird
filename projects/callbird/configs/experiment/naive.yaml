# @package _global_
#package global is neccessary!
defaults:
  - shared.yaml

module:
  _target_: projects.callbird.src.module.naive_multitask_module.NaiveMultiTaskModule
  num_combined_classes: ${module.network.num_combined_classes}
  metrics_ebird:
    _target_: birdset.configs.MultilabelMetricsConfig
    num_labels: ${module.network.num_combined_classes}

datamodule:
  _target_: projects.callbird.src.TODO.TODO
  transforms:
    target_columns: ["labels_ebird"]

logger:
  wandb:
    group: callbird_naive
    version: callbird_naive_${start_time} 