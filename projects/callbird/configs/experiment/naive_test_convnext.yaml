# @package _global_
#package global is neccessary!
defaults:
  - naive_common.yaml

train: False
test: True

ckpt_path: /workspace/logs/train/runs/callbird/convnext/2025-08-28_191655/callback_checkpoints/last.ckpt

datamodule:
  _target_: projects.callbird.src.NaiveTestDataModule.NaiveTestDataModule

module:
  network:
    model:
      num_classes: 134
      local_checkpoint: ${ckpt_path}