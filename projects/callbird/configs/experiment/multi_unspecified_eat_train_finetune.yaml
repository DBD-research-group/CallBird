# @package _global_
#package global is neccessary!
defaults:
  - multi_unspecified_eat_train.yaml

module:
  network:
    model:
      freeze_backbone: False
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-10-03_124528/callback_checkpoints/last.ckpt

trainer:
  devices: [0]