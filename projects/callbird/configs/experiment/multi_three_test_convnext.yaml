# @package _global_
#package global is neccessary!
defaults:
  - multi_three_common_convnext.yaml

train: False
test: True
ckpt_path: /workspace/logs/train/runs/OEKOFOR_TEST/convnext/2025-08-26_230706/callback_checkpoints/last.ckpt

trainer:
  devices: [1]

datamodule:
  _target_: projects.callbird.src.MultiTestDataModule.MultiTestDataModule

module:
  network:
    model:
      local_checkpoint: ${ckpt_path}