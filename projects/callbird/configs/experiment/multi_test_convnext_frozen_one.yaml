# @package _global_
#package global is neccessary!
defaults:
  - multi_common_convnext.yaml

train: False
test: True
ckpt_path: /workspace/logs/train/runs/callbird/convnext/2025-08-31_184211/callback_checkpoints/last.ckpt

trainer:
  devices: [1]

datamodule:
  _target_: projects.callbird.src.datasets.multi.MultiTestDataModule.MultiTestDataModule

module:
  network:
    model:
      local_checkpoint: ${ckpt_path}
      freeze_backbone: True
      variant: single