# @package _global_
#package global is neccessary!
defaults:
  - naive_same_classes_eat_train_finetune.yaml

datamodule:
  limit_samples: True

module:
  network:
    model:
      freeze_backbone: False
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-09-28_133713/callback_checkpoints/last.ckpt