# @package _global_
#package global is neccessary!
defaults:
  - naive_unspecified_convnext.yaml

train: True

datamodule:
  _target_: projects.callbird.src.datasets.naive.NaiveTrainDataModule.NaiveTrainDataModule
  filter_naive: /workspace/projects/callbird/blacklists/unspecified_train.txt