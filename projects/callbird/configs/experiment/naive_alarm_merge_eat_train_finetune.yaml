# @package _global_
#package global is neccessary!
defaults:
  - naive_alarm_merge_eat_train.yaml

module:
  network:
    model:
      freeze_backbone: False
      local_checkpoint: /workspace/logs/train/runs/callbird/convnext/2025-10-02_130852/callback_checkpoints/last.ckpt

trainer:
  devices: [3]